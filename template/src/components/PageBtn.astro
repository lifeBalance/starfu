---
import Card from '@components/Card.astro'
const {
  href,
  label,
  direction = 'next',
} = Astro.props as {
  href: string
  label: string
  direction?: 'prev' | 'next'
}
const isPrev = direction === 'prev'
const cardClass = `inline-flex items-center gap-2 w-full px-3 py-3 sm:w-[45%] sm:px-5 sm:py-4 ${isPrev ? '' : 'justify-end text-right'}`
const aria = `${isPrev ? 'Previous' : 'Next'}: ${label}`
---
<Card href={href} rel={direction} ariaLabel={aria} class={cardClass}>
  {isPrev ? (
    <>
      <svg class="text-sd-muted text-2xl sm:text-3xl shrink-0" width="1em" height="1em" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
        <path d="M15.41 7.41L14 6l-6 6 6 6 1.41-1.41L10.83 12z"/>
      </svg>
      <div class="min-w-0">
        <p class="text-xs sm:text-base text-sd-muted">Previous</p>
        <p class="text-base sm:text-xl leading-snug">{label}</p>
      </div>
    </>
  ) : (
    <>
      <div class="min-w-0">
        <p class="text-xs sm:text-base text-sd-muted">Next</p>
        <p class="text-base sm:text-xl leading-snug">{label}</p>
      </div>
      <svg class="text-sd-muted text-2xl sm:text-3xl shrink-0" width="1em" height="1em" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
        <path d="M8.59 16.59L13.17 12 8.59 7.41 10 6l6 6-6 6z"/>
      </svg>
    </>
  )}
</Card>
